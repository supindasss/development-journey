"""Task 21 || Tuple, Set & Dictionary || 23-02-2026
2. Create a tuple of numbers and find the maximum and minimum values without converting it into a list."""
tuples=(18,25,14,12,28,44,56,49)
maximum=max(tuples)
print(f"the maximum value in the tuple is {maximum}")
minimum=min(tuples)
print(f"the minimum value contain in the tuples is {minimum}")